version: 2
jobs:
  build:
    machine: true
    
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
      - run:
          name: retrieve code from BB
          command: git clone ssh://git@innersource.accenture.com/tpmmdshj/sampledx.git